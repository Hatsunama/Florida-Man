# Security source coverage — 5 September 2026

No additional reportable security vulnerability was established. This pass closed the 55 full-read gaps in the original 54-path receipt, fully re-read all 36 paths that initially retained only an earlier receipt, and reviewed the later QA client and main09 dialogue changes. It also fully read all 22 historical generator scripts and verified their subsequent retirement. It did not run application code, tests, builds, network calls, external apps, or security scan initialization/completion. The refreshed receipt closed at 2026-09-05 07:00:27 UTC.

| Receipt | Exact path count |
|---|---:|
| Original full-read receipt recovered from this task | 54 |
| Previously unread authoritative-scope files fully read | 55 |
| Late-added QA client, pagination helper and test fully read | 3 |
| Combined authoritative scope | 112 |
| Runtime Lua files within that scope | 78 |
| Additional asset guidance and historical arithmetic source fully read | 3 |
| Historical generators fully read, then retired with identical bytes | 22 |
| New historical-generator README fully read | 1 |
| Deduplicated full-read paths | 138 |
| Paths fully read during this supplemental pass, including refreshes | 138 |

The exact sorted paths, original/supplemental/refresh sets, SHA-256 values, and resolved questions are in [the machine-readable receipt](REAUDIT_SECURITY_COVERAGE_2026_09_05.json). Every file in the current authoritative scope has been fully read during this supplemental pass; there is no prior-receipt-only remainder. GameService was fully re-read after the dodge-guard change. The QA client helper was fully re-read after its module-path correction. Tagline, the new DialoguePagination helper, its test, and both current READMEs were fully read after the main09 changes and matched their owners' final hashes. Final hash comparison found no unreviewed source drift. Hashes identify reviewed versions; they are not substitutes for reading source.

The applicable attacker is a modified Roblox client authenticated as a player. The current product admits one owner per server and has no reviewed player-to-player commerce or shared progression surface. Changing only that owner's solo progression is treated as a gameplay integrity defect, per the supplied reporting context; it does not establish a separate security victim. No additional threat-model or SECURITY.md policy was supplied or resolved for the reviewed directories.

| Boundary checked | Source evidence and counterevidence |
|---|---|
| Client intent → server admission | SessionService.lua:8–18 admits one owner. GameService.lua:46 checks that owner before bounded command work; its position-sensitive ingress uses spatialAction at line 106. Remaining UI/controller files emit requests or present snapshots; local module-table changes do not change the server's catalog or transaction state. |
| Authenticated player → cloud record | MetaService.lua:112–137 derives the storage key from the authenticated Player.UserId, starts nonwritable, and grants write status only after adapter acquisition succeeds. ProfileAdapter.lua:44–78 checks the live lease owner, expiry, expected revision, normalized data, and exact retry token in the provider update transform. There is no client-selected datastore/key/provider path. |
| QA command → runtime mutation | StudioAuditBridge.server.lua:4–7 creates a BindableFunction only in an unpublished Studio server and parents it under ServerScriptService. StudioAudit.luau:22–42 rechecks server Studio, PlaceId 0, admitted owner, a loaded nonwritable profile, and a ready living character for each mutating helper. The bridge allowlists names and fixed argument shapes. It exposes no network remote or code-evaluation parameter. |
| Client QA command → normal input | StudioClientAudit.client.lua gates a local BindableFunction on Studio/client/PlaceId 0 and permits only Read, ScanAudio, and MovementSmoke. Scans and output records are bounded. MovementSmoke invokes normal Input.Intent and observes server events, with cleanup and a deadline; it adds no direct physics, state, reward, profile, or provider mutation. No test runs automatically. |
| Accepted dialogue → presentation | Tagline.lua retains complete server-authored messages and Notes entries, waits for valid viewport/text bounds, and calls the pure DialoguePagination helper. Grapheme byte offsets move only when consuming a page; hidden/modal presentation pauses timers. The helper/test add no remote, storage, entitlement or progression authority. Engine readability is not established by this source review. |
| Source → release/QA artifact | The default project maps src and excludes qa, scripts, tests, and scratch. validate.py:89–93 distinguishes source stability/check success from engine acceptance. build_studio_audit.py:69–99 requires the exact passing source manifest, adds QA only to the derived project, hashes all three QA sources, and checks stability before/after packaging. CI has read-only contents permissions and no publication/deployment step. |
| Catalog → generated world/models | WorldBuilder.BuildStage resolves an authored stage ID at line 1133; dimensions, loop bounds, prompts, and hazard attributes come from fixed catalogs. EnemyFactory resolves only fixed shapers and approved ArtAssets templates. No client-provided model, asset ID, source code, filename, URL, or subprocess command reaches those constructors. Presentation parts and effects do not provide a cloud/authentication authority path. |
| Local tools → process/filesystem | Current scripts and workflow were fully refreshed. Tool downloads use pinned SHA-256 checks; subprocesses use argument arrays. Paths and executable selection are local operator inputs. Catalog evaluation intentionally evaluates repository-owned Lua on the developer/CI host; it is not reachable from game clients. No supported injection, SSRF, unsafe remote deserialization, secret disclosure, or cross-principal resource-exhaustion path was found. |

Three concrete informational issues were sent to the parent task. EnemyFactory's boss constructor still spawned a callback using removed variable `weak` at the inspected lines 903–910; the parent deleted that callback and the corrected ending was re-read. The asset naming checklist still prescribed -Z while the imported model contract uses +X; the parent aligned the checklist and it was fully re-read. The RequestDodge ingress could grant a fresh movement allowance before validating the starting position; the parent added the same spatialAction guard used by the other position-sensitive actions, and the entire corrected GameService file was fully re-read. None establishes a meaningful security victim beyond the admitted owner's solo game. The previously identified movement weaknesses remain gameplay defects, documented separately in [the movement review](REAUDIT_MOVEMENT_2026_09_05.md).

Source drift handled during this pass: WorldBuilder was read after retirement of FM_SafetyPad; init.server.lua was refreshed after the invisible/noncolliding spawn-marker change; HUD.lua and CaptainSteveUI.lua were refreshed after the Tab binding and pending-only retry changes. The EnemyFactory correction was checked as the known deletion after its full read. Any later source edits require checking the recorded hashes and reviewing the changes.

All 22 historical `_gen` scripts were subsequently fully read as an ancillary technical-debt review: 4,258 lines, including their embedded legacy Lua. Every script contains source/report writes. Whole-file generators can restore obsolete movement, combat, interaction and audio code; marker-based migrations can fail partially; `counts.py` modifies source; old evidence generators can reproduce copied equations or fixed observations. No required safe read-only helper or exposed cross-principal execution path was found. The parent renamed all 22 to `.py.retired` in place and added a README. This review verified all old entrypoints absent, all pre/post hashes identical, and fully read the README. The exact original-to-current mapping and recommendations are in [the legacy review](REAUDIT_LEGACY_GENERATORS_2026_09_05.md) and [its hash receipt](REAUDIT_LEGACY_GENERATORS_2026_09_05.json). Retirement prevents stale `.py` invocations; deliberate Python execution of retired text remains possible.

The final filesystem inventory included ignored files: 2,287 paths outside .git, comprising 138 fully reviewed paths and 2,149 excluded/unreviewed paths. [The exact exclusion inventory](REAUDIT_SECURITY_COVERAGE_2026_09_05_EXCLUSIONS.json) identifies every remaining path and reason: generated validation/QA/place/catalog outputs, Python bytecode, empty asset placeholders, historical prose/screenshots/evidence, the ignored root place and its lock, and the review artifacts. There are no unclassified remainders or unreviewed current authoritative files. The retired scripts now have ancillary full-read receipts and are no longer in the unreviewed exclusion set. The historical `docs/audit-evidence-2026-09-04/camera_repro.py` was also fully read because it is source rather than prose. Git metadata/history was outside scope and not enumerated.

This receipt does not establish Roblox replication/physics behavior, live DataStore contention or ambiguous-commit recovery, deployed permissions/admission, package-to-running-instance identity, performance, asset loading, or campaign correctness. Those require the parent task's separate runtime and release evidence. No claim of “all gameplay correct” follows from an empty security finding list.
