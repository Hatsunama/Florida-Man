# Florida Man — consolidated re-audit, repair status and completion plan
## 5 September 2026

**The original opening failure is explained, and scoped Studio tests establish bonfire/NPC E, dodge, Return, native Space jump, Hub Tab and movement through the real input path. The full gameplay and release mission remains open.** The latest main09 package passed 78 runtime source files, 15 behavioral suites and 112 checks; derived QA09 passed six checks. Main09 fixes the dialogue fragmentation actually found on 08, using whole-message retention and lazy valid-layout pagination. That repair has source/rule proof but **no 09 engine check**. QA08's stage/lifecycle/Active-combat smoke was not run because commit headroom fell below the host threshold. The normal campaign, native movement-device matrix, complete conversation/visual acceptance, live persistence and deployment remain open.

The user's requirements remain binding throughout: **no audio; every necessary conversation uses small popup text; reduce screen crowding; preserve the existing game; work within the host's RAM limit; do not publish during this audit.** This document separates completed source work from the engine behavior actually observed. Runtime testing is stopped under the recorded memory threshold; future engine results require a new matching evidence row.

## 1. What failed, what is established, and what is still unknown

The user's failing session opened the old `artifacts/validation-07/FloridaMan.rbxlx`, SHA-256 `1613551b3b39f80c1d2dc9120ba76ed94e305b022f75c64244fdf40ddb33f2c4`. Its 02:26 UTC Studio log reports:

> Can only call Network Ownership API on a part that is descendent of Workspace

The same unsafe ordering existed in source: character setup marked the character bound, removed its assembly listener and called SetNetworkOwner before checking Workspace ancestry. The exception prevented characterReady=true. Bonfire and NPC handlers correctly required readiness, so both were unavailable; the location-oriented rejection message concealed the real initialization failure.

GameService now validates ancestry and ownership eligibility, catches assignment failure, and commits readiness only after setup succeeds. Character/ancestry observers and a throttled session-owned retry cover late assembly and transient physics eligibility. Replacement/release cleans those observers. The client also retries late Humanoid/HRP/Animator/motor arrival, instead of permanently returning after a timeout.

The first repaired package produced a usable game frame. Native E entered Daytona at HP100. A real Shift dodge moved approximately 18 studs and remained displaced after settling. The mouse-opened combat menu clearly stated “Run continues”; its second confirmation returned to the hub. These are useful observed successes, but they concern a specific first-pass build.

A second live failure was found: O did not open the menu in Hub or Active, and both attempts logged “Action zoomOutAction is not handled.” Raising FM_Options to ContextActionService priority 3000 passed source validation, but O still reached Studio zoomOutAction in the main05-source session. Main08 includes Tab with O retained as an alias. Native Tab opened and closed Options in the identified QA08 Hub session; Active, required-modal arbitration and D-pad-left remain separate checks. O is not reported repaired by that Tab result.

The actual boot log at 06:09:42 verified main05 identity a449d9b962a4…. Invoking the existing server-only QA bridge succeeded where direct Command Bar require had returned a session-owner guard refusal. NearCaptainSteve explicitly relocated the player; pressing native E then opened the shop, and Close revealed a small Captain Steve popup. A server ScanAudio inspected 2,155 instances with no findings and no truncation. This establishes the checked server scope, not client-only silence or experience voice configuration.

RunStageSmoke was executed in QA06 and returned 20 case records, but failed its spawn world-support assertion and the aggregate JSON log was truncated. No all-stage progression pass is established. The assertion requires the first collidable ray hit to be inside GameWorld. Persistent collidable FM_Spawn and an 80-stud FM_SafetyPad outside that stage-owned world were found. This can explain the ownership assertion without establishing absent physical support or failed playability. Main08 makes the spawn invisible/noncollidable and retires bootstrap support after world construction. Its source checks pass; the diagnostic was not rerun in QA08 because commit headroom was only 1.45–1.61 GiB against a 2 GiB stop threshold.

The [durable main05 engine-events excerpt](reaudit-evidence-2026-09-05/build05-engine-events.txt) now preserves the native Space trace: 1,794 samples over 29.994905 seconds, starting and finishing Y=4.176333427429199, apex Y=11.499826431274414, rise=7.323493003845215 studs and zero reset-revision changes. The watched attack/skill/swap/dodge commands array is empty; jump is local movement, so that does not contradict the observed jump. X stayed essentially constant. This is measured jump/return evidence, not a walking result or a full short-hop/device/rig matrix.

The baseline and first repaired screenshots show fewer persistent HUD/world instructions, but their viewports differ, so they are not a matched-viewport visual approval. Build03 also shows orange/yellow hub glare. The root observed a muted Hub in main08 after lighting changes, but did not preserve an exactly matched unobstructed Hub frame. Final lighting, readability and art acceptance remain open.

QA08's [durable events](reaudit-evidence-2026-09-05/build08-engine-events.txt) verify main08 identity 3343bd4af705… at boot. The client scan inspected 2,342 nodes with zero Sound/AudioPlayer sources, no other Audio-class objects and no truncation. MovementSmoke drove actual Input.Intent for 0.8 seconds of rightward motion, then released and jumped: deltaX=6.610567092895508, jump rise=6.362632513046265, landed=true, elapsed=1.53 seconds and stable motion-reset revision. This is real controller/server composition, not native held-key binding proof. Attack, skill and dodge were unavailable in Hub, so no combat intent was submitted and allActionsAccepted=false. Native E then opened Steve's shop at the reached position without a QA relocation in that session.

QA_CLIENT_READ exposed the popup body as “retry.” despite its 376px content width. Source tracing found that Tagline.Show permanently split accepted messages using the initial camera's zero viewport, reducing measurement width to 50px. Main09 now retains complete messages/history, waits for valid viewport and actual text dimensions, and paginates from UTF-8 grapheme boundaries without consuming text until advance. Resize reflows the unacknowledged page. Production pagination tests passed as the 15th suite; actual cold-start reading on 09 remains unexecuted. The [structured runtime record](reaudit-evidence-2026-09-05/runtime-results.json) preserves 13 log records, native observations, truncation and limits.

### 1.1 Matching-build evidence ledger

“Observed” below means the root reviewer performed the action in Studio. “Source pass” means the named static or pure-rule check passed. Neither label implies the other.

| Scenario | Build and evidence | Current verdict |
|---|---|---|
| Original cold start | validation07 package/log; [baseline hub](reaudit-evidence-2026-09-05/baseline-hub.png), [misleading E rejection](reaudit-evidence-2026-09-05/baseline-e-rejected.png) | **Observed failure:** ownership-before-Workspace exception prevented readiness |
| Usable hub after startup repair | frozen build03 identity 7194054af4ab…; [hub capture](reaudit-evidence-2026-09-05/build03-hub.png) | **Observed usable frame.** This supersedes the earlier “no usable frame” resource history for this session |
| Native E at bonfire | same build03 identity; [Daytona capture](reaudit-evidence-2026-09-05/build03-e-started-stage1.png) | **Observed pass:** entered stage1 at HP100 |
| Accepted Shift dodge | same root live build03 session | **Observed displacement of about 18 studs that remained after settling.** No comprehensive latency/speed/rig matrix implied |
| Combat menu and abandon | same root live build03 session | **Observed mouse-opened menu and confirmed Return → Hub.** Two activations required |
| Main05-source boot identity | actual boot log at 06:09:42, identity a449d9b962a4… | **Observed matching identity.** Does not certify subsequent source edits |
| O / Tab shortcut | O failed in 03 and05 despite priority3000; native Tab in matching 08 [Options capture](reaudit-evidence-2026-09-05/build08-tab-options.png) | **Observed Tab open/close in Hub.** Active, D-pad-left and modal arbitration open; no O success claimed |
| Native Space jump and return | main05-source [durable engine events](reaudit-evidence-2026-09-05/build05-engine-events.txt), QA_TRACE: 1,794 samples over 29.994905s; Y 4.176333→11.499826→4.176333, resets=0, commands=[] | **Observed numerical native jump:** rise 7.323493 studs and return. Separate08 Input.Intent short-release jump also landed; same-build held/released, rig/device matrix open |
| Captain Steve E → shop | main05 bridge relocation/native E/Close; main08 [walk then E capture](reaudit-evidence-2026-09-05/build08-walk-npc-e.png) | **Observed native E shop opening after Input.Intent walking/jump without relocation in 08.** Close/small-popup sequence observed on 05; full dialogue now has a separate08 fragmentation defect; LB/touch open |
| Hub glare repair | glare visible on 03; source lighting changed by 05; root observed muted 08 Hub | **Partial visual observation; formal matching-viewport approval open.** No matching unobstructed08 capture |
| R6/R15 fallback animation, delayed assembly | second-pass source and pure rig geometry checked | **Runtime unverified** |
| Existing server QA bridge | direct Command Bar require returned session guard refusal; existing server BindableFunction invocation succeeded | **Observed bridge invocation.** No guard weakened, client remote added or test auto-run |
| All-stage service smoke | executed QA06 returned 20 records, failed support assertion; aggregate log truncated | **No progression pass.** Main08 bootstrap cleanup is source-validated; QA08 stage/lifecycle/Active smoke unexecuted due commit headroom below 2 GiB |
| Server/client audio scans | main05 server:2,155 nodes; main08 client:2,342 nodes; both no findings and truncated=false | **Observed clean inspected scopes only.** Client08 reports no Sound/AudioPlayer/other Audio-class objects. All verbs/stages, past/future audio and experience voice configuration remain open |
| Movement through actual Input.Intent | main08 QA_MOVE_SUMMARY: deltaX 6.610567, jump rise 6.362633, landed=true, elapsed 1.53s, motionResetStable=true | **Observed movement/jump composition pass.** No native held-key input was generated; attack/skill/dodge unavailable and not attempted in Hub |
| New client QA endpoint and dialogue | QA08 Read/ScanAudio/MovementSmoke invoked; preserved body “retry.” at 376px | **Observed 08 popup defect; source/rule repair passed in 09.** Whole-message/lazy-pagination engine reading remains open |
| Continuous normal campaign | no ordinary-input twenty-stage record | **Unverified** |
| Controller/touch, persistence, performance, silence configuration and deployment | separate gates below | **Unverified where no explicit row says otherwise** |

### 1.2 Immutable package references

| Artifact | Recorded source/build identity | What its report establishes |
|---|---|---|
| [Main03 report](../artifacts/reaudit-build-03/report.json) | Identity `7194054af4ab8291cb59c839854c91accb3d37863b39135fcc2c47b4f71d1ecc`; main package SHA `cf8b30b1e4f8390ef5cabfc0e63ea5baae23b1daf23fdb44db26ab30b6124547` | 97 successful pipeline checks for that earlier source. Root used the matching source identity for the live03 observations |
| [Main05 report](../artifacts/reaudit-build-05/report.json) | Identity `a449d9b962a406b49f58a009bebcd02abb7c29fd6445a8d33e533fa6314f728f`; main package SHA `dc83da6ad5410f599f68db6aebaff800aac298c163698a643e33269a2f3d8c70` | passed=true, sourceStable=true; **77 runtime source files, 14 suites, 109 successful checks**. The 91 compile entries comprise 77 source files and 14 test files. engineAcceptance explicitly says the validator did not perform it |
| [Main08 report](../artifacts/reaudit-build-08/report.json) | Identity `3343bd4af705195a25501090dd63ed3c0d1f10c81a4af636358e1b2cc7127465`; package SHA `683fb58306fa1899729e55f2f06ef7587323ad8c5ea8730b629b94c4bab853dc` | Historical package corresponding to latest observed engine source: passed/sourceStable, 77 runtime files, 14 suites, 109 checks. Matching QA08 runtime results remain separate |
| [Main09 report](../artifacts/reaudit-build-09/report.json) | Identity `d5e040a6ef1427f3b5250a56d8d22d51180cadc15cdf69b42e4fdc8fb2b5a8e9`; main package SHA `573103ba2f1c7adec312b738034a146b5ab45f47d5b677fa9a091f242e7a93fa` | **Latest static source package:** passed=true, sourceStable=true, 78 runtime files, 15 suites, 112 checks. Adds whole-message/lazy-layout pagination and its production rule suite. **No 09 engine execution** |
| [QA09 report](../artifacts/reaudit-studio-09/report.json) | Same main09 identity; QA package SHA `c538c7aaacc4b40abaf951022ef3a358ec4a8294ed4df33b5cfd533580165a3e` | Six passing isolated checks; unchanged default project/stable inputs. Baseline report SHA `1018927768bdc234e186c2db3c4a2d4863f32abe1c499fe9e125605045a239ba`. engineAcceptance=not-executed; no runtime result attributed |
| [QA08 report](../artifacts/reaudit-studio-08/report.json) | Same main08 identity; QA package SHA `86b3e72fb44bb28e720e3817f08bea2bd42fc0706b5ad744bcc1c562b2fb5bee` | Six successful checks: server helper/bridge/client compile, sourcemap, types and package; sourceStable=true, default project unchanged. Build-time engineAcceptance=not-executed remains immutable; later boot/actions/scans are above |
| [QA06 report](../artifacts/reaudit-studio-06/report.json) | Same main05 identity; QA package SHA `1a8b7f8a187e07e5e3dbca55c667b6e469936e967fd1b9700beb5cfed57aa8db` | Five successful QA compile/bridge/sourcemap/type/package checks; baseline main05 report hash; separate helper/bridge hashes; default project unchanged. Its build-time engineAcceptance=not-executed is preserved; subsequent runtime observations and the failed smoke are recorded separately above |

The QA binaries are intentionally different from their main packages. QA06 shares main05 source identity; QA08 shares main08; QA09 shares main09. Each report records separate helper/bridge/client and package hashes. A filename, recent timestamp or old passing report is insufficient provenance. Options displays a short build identifier. The dialogue repair after QA08 belongs to 09's fresh static report but has no 09 engine result; do not attribute 08 runtime observations to that repair.

## 2. Audit coverage and confidence

This consolidated report draws on the independently scoped reports below and the root's direct runtime observations:

- [Client/input/UI audit](REAUDIT_CLIENT_2026_09_05.md): actual baseline screenshot plus client source, interaction ownership, modal/focus rules, accepted feedback, delayed rig assembly, silence, touch and accessibility repairs.
- [Gameplay/world/content audit](REAUDIT_GAMEPLAY_2026_09_05.md): all stage definitions and catalog connections, actual producers/consumers, support geometry, hazards, enemy facing, delayed healing, pickup completion and content honesty.
- [Movement authority audit](REAUDIT_MOVEMENT_2026_09_05.md): server coordinate acceptance, finite action allowances and the actual pure envelope tests.
- [Architecture/responsibility audit](REAUDIT_ARCHITECTURE_2026_09_05.md): fresh trust model, 39 fully read runtime source files and 15 fully read project/tooling/QA/reference files, with explicit partial/unreviewed bodies.
- [Release acceptance matrix](RELEASE_ACCEPTANCE.md): engine, campaign, provider, device, silence, resource and deployment requirements, carried forward below.
- Client and gameplay hash manifests linked from their reports identify intermediate review states. Immutable validator manifests are the package references.

The ancillary historical-generator review separately read 22 scripts/4,258 lines. These were outside mapped current source and unused by current CI/builds. [Retirement mapping/evidence](REAUDIT_LEGACY_GENERATORS_2026_09_05.json) and [_gen guidance](../_gen/README.md) preserve original names/hashes and scope; all 22 old entrypoints are absent and retired bytes match. No generator was run to reproduce destructive overwrite behavior. This is technical-debt closure, not an exposed security-finding claim.

These counts overlap and must not be summed into a claim that every file was independently reviewed several times. EnemyFactory's full large implementation was not fully read by the architecture worker; its boundary review is identified as partial there. A grep match, import, sourcemap entry, compiler pass or inventory row is not full-file review.

**Security source coverage is sealed at 2026-09-05 07:00:27 UTC.** The [coverage report](REAUDIT_SECURITY_COVERAGE_2026_09_05.md) and [hashed receipt](REAUDIT_SECURITY_COVERAGE_2026_09_05.json) record 138 fully reviewed current paths: 112 authoritative files (78 runtime), 3 additional asset/arithmetic-source files, 22 retired historical scripts and the new _gen README. All authoritative files were fully read during the supplemental pass; there is no prior-receipt-only or unreviewed-authoritative remainder. The final inventory has 2,287 paths: 138 reviewed and 2,149 [explicit exclusions](REAUDIT_SECURITY_COVERAGE_2026_09_05_EXCLUSIONS.json), with no unclassified remainder. Final main09 dialogue/helper/test and README hashes were checked.

**No additional reportable security vulnerability was established within that reviewed scope and threat model.** That conclusion does not certify gameplay integrity, replication/physics, readability, live DataStore behavior, deployed settings, assets, performance or campaign correctness. The supplemental source reviewer ran no app code, tests, builds, network operations or formal scan-lifecycle tools; source coverage and any broader scan workflow must not be conflated. Historical-script retirement is documented in its [review](REAUDIT_LEGACY_GENERATORS_2026_09_05.md) and mapping, with no script execution or content deletion.

Tooling failure injection has separate [actual results](../artifacts/reaudit-tooling-failure-final/checks.json). Missing validator tools and missing QA baseline each returned failure, invalidated seeded stale success and released their lock. QA output aliasing was rejected by its exact protection message, with main09's report hash unchanged. An initial harness invocation omitted required --tools-dir; [that harness error](../artifacts/reaudit-tooling-failure-final/initial-harness-error.json) is preserved and is not source-failure or guard-success evidence. Only the corrected invocation establishes the alias case.

The source/pure checks also do not establish actual engine scheduling, CFrame collision results, network-ownership timing, native prompt arbitration, controller focus, rendered glyphs, animation permissions, target-device frame time or DataStore durability. The initial live exception and O-key failure are direct examples of those evidence gaps.

## 3. Consolidated defect and repair register

Priorities describe player or release impact: P0 prevents the opening experience; P1 blocks or misrepresents a core action/progression/authority boundary; P2 affects mechanics, consistency, readability or release reliability; P3 removes false descriptions and dead paths. “Source repaired” remains open for the stated runtime acceptance.

| ID / severity | Concrete trigger and defect | Implemented repair and owner | Verification / remaining closure |
|---|---|---|---|
| R01 / P0, RC-01 | Character setup reaches a physics ownership API before Workspace ancestry; E/NPC readiness never commits | GameService/SessionService: guarded assignment, ancestry/part observers, successful readiness commit, scoped retry and cleanup | Original failure observed;03 bonfire E and08 native NPC E after Input.Intent approach observed. Delayed assembly, repeated respawn and native movement/device acceptance remain open |
| R02 / P1, RC-02 | Native interact and CAS both own E; default controller X conflicts with Attack while text advertises LB | InputController uses native E/LB ownership; world/turtle prompt constructors declare the same keys; modal/phase readiness gates prompts | Keyboard bonfire observed on 03. LB, touch target parity, two nearby prompts and modal rejection still open |
| R03 / P1, RC-03/G01 | HUD, repeated world instructions, platform overlays and persistent dialogue compete over the player | One 44px status strip; details in Notes; one measured small popup; duplicate CoreGui and hub labels removed; remaining spatial labels bounded/occluded | Baseline/03 comparison observed. Smallest touch viewport, large fonts and long conversations still need actual rendering checks |
| R04 / P1, RC-04 | Menus stack, gamepad moves behind UI, full-bag replacement loses focus; a new single-owner panel could erase Draft through O | UiKit owns one panel; modal cleanup and explicit replacement focus; Options refuses required-modal/Draft ownership; held input clears | Production layout suite passes. Controller-only Draft/Back/Skip/Retry and repeated menu transitions unverified |
| R05 / P1, RC-05 | Predicted combo differs from slow accepted combo; rejected dodge can move before server approval | Accepted attack event supplies combo/facing/VFX/readiness. Dodge displacement and animation start from the accepted event | Real03 dodge observed. Slowest three-hit combo, denied actions and latency matrix open. Current client does not play an immediate predicted dodge animation |
| R06 / P1, RC-11 | O loses to Roblox/Studio zoomOutAction in Hub and Active | Priority3000 did not fix05 O; root added Tab with O alias in 08 | **Native08 Tab opens/closes Hub Options.** Active, required-modal and D-pad-left checks open; O remains a known Studio conflict |
| R07 / P1, RC-06/G17 | Client timeout abandons late parts; old R6 feet calculation excludes leg length | Event-driven client binders, stale-character guard; shared CharacterGeometry used by mover, authority, hazards, tutorial and combat placement | Actual pure R6/R15 geometry and source pass. Delayed rig assembly, grounded jump and low target hit checks open |
| R08 / P1, movement/G03 | Client-owned coordinates reach direct or delayed spatial consumers before acceptance; rejected movement could skip hazard damage | MovementAuthority validates synchronously before positional commits; delayed enemy telegraph validates again; hazard evaluation continues at corrected coordinates | Pure envelope and source trace pass. Malformed motion during committed attacks/hazards and legitimate latency cases open |
| R09 / P1, movement | Fixed permissive speed grants sustained excess motion while rejecting some legal dodge-plus-walk windows | Elapsed normal motion, finite replication credit, expiring server-issued excess-distance/impulse grants, exact trusted teleport baselines | Envelope suite covers legal/illegal sequences.03 dodge observed. Mixed pad/wind/dodge, fast builds and hitches require engine traces |
| R10 / P2, G04/G05 | Overlapping hazards choose first hash-table entry; GREASY also resists unrelated sand/water/paperwork | HazardRules deterministically merges strongest mitigated damage/impulse and clamped motion; typed oil/environment slow channels | 5,040 overlap permutations and slow policy cases passed. Actual HP, impulse timing and expiry across death/stage changes open |
| R11 / P2, G06 | Procedural enemies face sideways or fail to rotate when stationary in range | Per-family forward-axis metadata and one pivot rule; rotated root world extents in hit geometry | Pure fourteen-family facing cases pass. Rendered front/attack/collider alignment and imported +X kit checks open |
| R12 / P1, G07/G17 | Root-based coil/projectile placement misses low grounded enemies, especially R6 | Shared feet geometry; skill owns offset, combat uses exactly rendered volume dimensions | Pure placement cases pass. Coil/ranged attacks against crabs/snakes on ground/ledge and through walls unverified |
| R13 / P2, G08 | Delayed projectile/coil lifesteal changes server HP after the command snapshot, leaving HUD stale | Heal from actual applied damage, publish only when HP changes | Pure heal/overkill/clamp cases pass. One shot with no further input must visibly heal at impact |
| R14 / P2, G09 | Player skips the stolen can but exits Daytona and reaches story claiming recovery | ProgressionRules requires Cold One only for its declared stage; one exit reminder; accepted snapshot and compact objective | Predicate tests pass. Ordinary-input skip → rejection → backtrack → pickup → exit still open |
| R15 / P2, G10 | Barge set-piece collider fills the real ground-plan gap | LayoutPlan.SupportedSlices clips deck supports to non-gap spans | Exact production Barge gap/deck slices tested. Falling/crossing from both directions and other set-piece collider unions open |
| R16 / P2, RC-07 | Empty animation slots produce no procedural run legs; R6 motor aliases absent; stock Animate ownership unclear | Late motor discovery, R6/R15 aliases, idle/run/airborne fallback, stock claim only after usable limbs | Source/types pass. Real rigs, action transitions, stock-track conflict and Reduce motion articulation open |
| R17 / P2, RC-08 | All hazard surfaces display floating DANGER, including inactive and non-damaging ones; particle setting can be undone by later enabling | Semantic surface stripes/flow arrows/slow marks, active/enabled/direction observation; local particle visibility preserves server emission state | Catalog cue suite passes. Timed/pool lifecycle, glyph visibility and live option toggles open |
| R18 / P2, RC-09 | Touch buttons look ready when unavailable; hybrid devices get unnecessary overlays; cooldowns hidden | Shared last-device policy; button disabled/pending/countdown state; hide releases held input | Seven device-policy cases pass. Multi-touch, hybrid switching and focus/target size open |
| R19 / P2 — stale validation evidence and QA output collision | Failed validator rerun can preserve earlier success; QA output can overwrite baseline report | Initialize failed evidence before work, final error/status write; reject same resolved QA output/baseline directory; shared lock and hash matching | **Tooling regression closed:** [three negative cases passed](../artifacts/reaudit-tooling-failure-final/checks.json): missing tools/baseline invalidate stale success and remove locks; output alias refuses with exact guard and unchanged main09 report SHA. No engine inference |
| R20 / P3, G13/G14/G15 | Solo aggro promise has no alternate target; static pier/instant rescue named as collapsing/escort; decorative boss weak point implies nonexistent damage rule | Removed ineffective special/dead branches, truthful names and descriptions, muted ordinary armor; numeric intended item stats preserved | Source/caller inventory and compilation pass. Final content/visual review and item-feedback usability remain open |
| R21 / P2 diagnostic — bootstrap support ownership | QA support ray requires a GameWorld hit but persistent collidable FM_Spawn/FM_SafetyPad sit outside GameWorld | Main08 makes spawn invisible/noncollidable and retires bootstrap support after world construction; diagnostic records actual ray hit | Assertion failure observed in 06; physical missing-floor failure **not** established. QA08 per-stage rerun unexecuted due memory threshold |
| R22 / P1, RC-12 — startup dialogue fragmentation | Accepted startup text is queued before viewport is usable; Show stores 50px fragments permanently. 08 read shows “retry.” in376px body | Main09 Tagline retains full messages/history, waits for valid actual layout, delegates pagination to pure DialoguePagination with grapheme offsets and advances only on acknowledgement | **Observed 08 defect; 09 compile/types and production pagination suite passed.** Complete cold-start/NPC/story conversation reading remains unexecuted |
| R23 / P2 — obsolete source/report generators | 22 historical _gen/*.py scripts overwrite current source/reports; some restore audio/old physics, audit_verify fails to signal failed checks, and evidence finalizers hardcode success claims | Root reversibly renamed all 22 to .py.retired after absolute-child-path/hash checks; no execution or deletion; added _gen/README.md directing work to the current validator | **Retirement closed.** All 22 originals absent and retired content hashes unchanged; 4,258 lines fully read by security. Ancillary debt closure, not a runtime vulnerability or engine pass; main09 source hash unchanged |

Detailed triggers, post-fix source references and per-case assertions are in the linked workstream reports. Their source locations identify their recorded review snapshot; line movement in a later revision must not be mistaken for changed evidence.

## 4. Architecture and responsibility verdict by layer

The intended architecture is server-authoritative game truth with pure rules and a client presentation layer. Most repaired paths now follow that direction. The table records the exceptions and the evidence still required.

| Layer / owner | Correct responsibility and observed boundary | Verdict and exception |
|---|---|---|
| Entry/composition — GameService | Registers remotes, orders join/character setup and routes accepted commands to domain services | **Improved after R01.** Readiness ordering was an integration defect missed by static tests. Keep lifecycle work scoped and do not commit ready before physics setup |
| Admission/lifetime — SessionService | One owner before profile/world initialization; generations and named callbacks invalidate stale work | **Appropriate owner.** Second-player rejection, fast reconnect and cleanup require engine tests; deployment MaxPlayers=1 remains external |
| Accepted state — RunContext | Private server run table, phase/generation, snapshot projection and trusted transitions | **Appropriate direction.** Keep BuildRules/RunStats and CharacterStatePublisher separate; attributes and snapshots are projections, not a second authority |
| Pure gameplay rules | Geometry, feet dimensions, movement envelope, hazard merge, build derivation, progression, support slicing and telemetry policy | **Good separation with executable tests.** Pure success must not be reported as engine traversal, provider or native UI success |
| Transport — Remotes/GameService | Declared intent messages, type/phase/owner/sequence validation, separate ingress/rejection budgets | **Authority stays server-side.** Generation and public IDs are not secrets/capabilities. Adversarial/live rate behavior remains a release check |
| Spatial authority — MovementAuthority | Accepted coordinates, correction, bounded server-issued motion permissions | **Owns legality.** The envelope is a plausibility policy, not full server physics. Direct and delayed consumers must validate immediately before consequences |
| Combat — Attack/Combat/Skill/Swap services | Cooldowns, attack timeline, real damage/results, shields, actual-hit effects and cancellation | **Appropriate owner.** Client visuals consume acceptance. Stagger uptime, contact geometry and timing under real scheduling remain open |
| Enemy runtime/factory | Definition-driven spawn budget, hostile lifecycle, AI semantics, procedural/imported rigs and root collider | **Mixed current responsibility.** Factory/server animation still performs visual work. General step/floor/gap navigation is missing; profile first before moving cosmetic animation client-side |
| Layout/world construction | LayoutPlan owns supports/reservations/gaps; WorldBuilder constructs compatible parts and prompt metadata | **Repaired leak:** deck construction previously overrode ground truth. Other set-piece collider unions still need actual traversal/overlay inspection |
| Hazard definition/adapter | Catalog defines semantic effect; HazardRules merges it; HazardService owns overlap/lifetime and requests legal external motion | **Improved.** Typed slow and deterministic merge remove accidental behavior. Timed geometry, i-frame/impulse ordering and cleanup need live evidence |
| Stage/progression — StageFlow/ProgressionRules | Waves, rooms, pending hostiles, required pickups/rescues, final boss and idempotent reward completion | **Required objectives connected in source.** A UI acknowledgement never supplies completion permission. Normal campaign and both finale orders remain unverified |
| Inventory/shop/unlocks — ItemGrantRules, DraftService, HubService | Exact current offer, slots, duplicate/replacement rules, request sequence and affordability; persistent mutations require writable profile | **Appropriate server owner.** UI focus, response-loss/retry and full-bag user experience need runtime checks. Game-earned unlocks are not paid entitlements |
| Profile schema/queue/adapter — ProfileRules/ProfileQueue/ProfileAdapter/MetaService | Known-data validation, immutable queued operation/revision/lease policy; sole storage provider binding | **Layering is explicit.** Pure failure tests are not proof of real DataStore behavior. Read failures stay non-writable; migration/lease/rejoin/shutdown remain controlled-provider gates |
| Character projection — CharacterStatePublisher/RunStats | Accepted HP/stats/appearance/status attributes; feet dimensions shared through CharacterGeometry | **Appropriate projection.** Client model appearance has no damage authority. R6/R15 scaling must be verified in engine |
| Input/motion presentation — InputController/MovementController | Device intent, permitted local movement, native prompt coordination, accepted attack/dodge feedback | **Repaired arbitration/lifecycle defects.** The server remains responsible for legal motion. O repair, touch, controller and latency need independent live evidence |
| UI/dialogue — HUD/UiKit/Tagline/Draft/Steve | Small silent text, accepted-state display, modal/focus ownership and explicit requests | **Hierarchy simplified; lifecycle defect source-repaired in 09.** Queue/history retain full content; viewport-dependent pagination occurs at presentation time. Actual 09 reading/device acceptance remains open. Functional-menu/safe-phase/minimum-height limits remain explicit |
| Camera/animation/equipment/VFX | Camera framing and local feedback; procedural or permission-checked asset fallback | **Source paths exist.** Empty animation registry slots do not establish authored animation delivery. Imported asset permissions, contact sheets, glyphs and threat visibility are open |
| Silence — reserved character sound script + SilenceController | Suppress default and subsequent Sound/AudioPlayer playback locally; no audio options or gameplay dependency | **Source policy plus bounded 05 server / 08 client scans observed.** Neither proves all future effects or experience voice configuration |
| Analytics/diagnostics — AnalyticsAdapter/FunnelService/RuntimeMetrics | Bounded queues, allowlisted low-cardinality cohort, rolling samples and developer observations | **Non-authoritative and bounded in reviewed containers.** No permission depends on telemetry. Provider delivery and measured client FPS are still unverified |
| Packaging/QA — validator, build identity, isolated builder/helper/bridge | Sequential verified tools, immutable source/package evidence, guarded local diagnostics | **Evidence boundary improved.** QA is not shipped by default; its tests need explicit invocation. A passing QA build is not an executed smoke or campaign |

### 4.1 What does not apply to this repository

This is a Roblox/Luau experience. Expo, React Native, browser routing/DOM, web cookies, OAuth refresh tokens and a conventional web login/session implementation do not exist in the mapped runtime. No Expo migration or browser authentication layer is needed to repair this game.

No MarketplaceService receipt/purchase or paid-entitlement implementation was found in the mapped-source search. Personas, weapons, Sunburn and shop mutations are game-earned state with their real validation boundary on the server. A branded in-game badge is fiction, not authentication.

There is no LLM-agent runtime, model-provider selection, AI memory/control plane or third-party connector workflow in the mapped gameplay source. Roblox DataStore and Analytics are real provider boundaries and remain in scope. Publishing configuration and imported-asset permissions are also real external dependencies. No new provider, credential, audio system, paid feature or multiplayer mode was introduced to satisfy unrelated checklist terms.

## 5. Gameplay, content, visuals and balance coverage

### 5.1 Catalog connections

The gameplay workstream followed 20 stage definitions plus the hub, eight personas, 28 weapons, 28 items, six inscription sets and 34 enemy definitions (33 hostiles and one rescue ally) to their consumers. Source manifests are not evidence that every catalog entry has been exercised through player input.

- Weapons have actual melee, thrown and ranged branches; finite swept projectiles and generation cancellation remain connected. The finale weapon is earned for a later run.
- Each persona has an actual skill path and a three-strike moveset. The low-ground coil and R6/R15 placement corrections are included. Rarity modifies accepted damage/cooldown in the server build policy.
- Items derive flat stats and set effects from accepted inventory; real-hit specials use actual applied damage. Full-capacity enemy drops are declined under the existing rule; the quiet decline still needs user feedback review.
- Required pickups/rescues, midrooms, waves, pending hostiles and finale conditions belong to completion rules. Story, headlines, prompts and UI visibility explain those conditions but never authorize them.
- Procedural enemies cover fourteen shape families. Optional imported models are validated for geometry/unsafe contents and use a declared forward axis. Current empty animation slots and generic held-weapon shapes remain presentation limitations, not completed art-delivery claims.

Do not infer final difficulty from the catalogs. For example, the current common starter three-hit cycle has a source-derived uninterrupted ceiling of about 67.6 DPS before approach, input cadence, knockback, misses and retaliation. A generous damage build reaches multiplier 2.93 before rarity/situational effects; a mobility build can have a 0.4752-second dodge cooldown with 0.38 seconds of protection. These figures identify stress-test cases. They do not prove real boss time-to-kill, difficulty, exploitability or fun.

### 5.2 Stage acceptance matrix

Every row needs ordinary-input traversal. A successful isolated service smoke may be recorded in a separate column later; it must not change ordinary traversal to passed.

| Stage | Required live focus | Current normal-play status |
|---|---|---|
| Hub | Cold spawn/readiness; E/LB Steve and bonfire; saved loadout; glare; tutorial jump; silence |03 bonfire E;05 native Space rise/return; 08 Hub Tab, Input.Intent walk/jump, native NPC E without relocation and bounded client audio scan observed. Dialogue defect and remaining matrix open |
| 1 DaytonaHangover | Three waves/miniboss; pier step; skip/recover Cold One; first reward/unlock |03 entry observed; completion open |
| 2 BoardwalkChaos | Charger/flash direction; fryer ledges; timed oil; queued two-hostile cap | Open |
| 3 GasStationLegends | Slime facing; paperwork slow; first room lock; duplicate award safety | Open |
| 4 StripMallShowdown | Summoner capacity; boss/add cleanup; remaining adds block exit | Open |
| 5 DriveThruDisaster | Gator+X orientation; kill-triggered collar clue; unlock/reward once | Open |
| 6 CanalRun | Real gap, pads, water slow; burrow geometry; four item slots | Open |
| 7 SwampShift | Coil vs low enemies; lizard facing; oil/fire overlap; clue delivery | Open |
| 8 CypressCathedral | Summon cap; snake orientation; corpse/queue and repeated-cast cleanup | Open |
| 9 SludgeBayou | Deterministic overlap; dynamic puddle expiry and registry release | Open |
| 10 ConspiracyShack | Flyer/humanoid facing; evidence/story sequencing without label clutter | Open |
| 11 TurtleBeach | Five required rescues; reachable prompts/landings; kill-first/rescue-first; five slots | Open |
| 12 NestGuard | Four rescues; correct stage reset with run/lifetime totals preserved | Open |
| 13 GulfGulpGate | Conveyor direction/allowance; real midgate versus decorative gate | Open |
| 14 LabWing | Moving hazards, pipe launch, overlap mitigation and lab memo | Open |
| 15 PipeGauntlet | Raised-platform navigation; projectile obstruction; boss fire telegraph | Open |
| 16 LoadingDock | Hermit facing; raised docks/conveyors; sixth item slot | Open |
| 17 BargeCrossing | Actual split deck gap; wind; fall recovery; ground/flyer behavior | Open |
| 18 OilPlatformApproach | Dense effects; raised-walkway range/occlusion; clue sequence | Open |
| 19 HelipadHysteria | Opposing wind; no cumulative vertical drift; fast-build dodge | Open |
| 20 GulfGulpRig | Five waves; both boss thresholds; adds/ring; three turtles; both objective orders; rewards/credits once | Open |

### 5.3 Graphics and communication verdict

Build03 removes much of the baseline UI duplication and keeps the player/action area clearer. The captures use different viewport sizes; they support a structural reduction, not a formal matched-view visual approval. Glare was visible on 03. The root observed a muted 08 Hub but did not save the same unobstructed view, so final lighting/art acceptance remains open.

The intended communication hierarchy is one persistent status/objective strip, one nearby native interaction prompt, one small conversation popup, and transient accepted-impact feedback. Detailed build/equipment/history data is available on demand. Functional reward/shop menus retain scrolling and controller focus; they should not become another permanent gameplay overlay.

Necessary dialogue uses measured pages and retained Notes, with readable dwell and timers paused behind menus. QA08 proved eager startup measurement fragmented accepted messages. Main09 separates message/history retention from current layout, waits for usable actual dimensions and reflows from a retained grapheme offset; its production pagination suite passed. Actually read the longest conversations with Large text and Slow/Instant settings at the smallest supported viewport before closing this repair. Extremely short content height hides the popup rather than covering touch controls; the supported minimum-size decision remains open.

Do not add audio to compensate for unreadable cues. Test silence across default character spawning, movement/jump, combat, hazards, menu actions, stage transitions, death, rescues and credits. The experience's voice setting needs an explicit configuration check by its owner; do not silently change platform/privacy/provider settings.

## 6. Remaining concrete debt and uncertainty

| Priority / item | Evidence and impact | Next action and measurable closure |
|---|---|---|
| P1 acceptance — dialogue integrity after startup | Main08 rendered “retry.” as the 376px body; 09 whole-message/lazy-layout repair passed production pagination tests | Cold-boot identified 09 and read full startup/NPC/objective/finale dialogue, including resize, text size, advance and Notes. Preserve no-audio/small-popup constraints |
| P1 acceptance — device/phase opening flow |08 Hub Tab, Input.Intent walk/jump and native NPC E without relocation observed; O Studio conflict persists | Test native held keys, Active/Draft shortcut arbitration, controller/touch, same-build held/released jump and delayed rigs. Keep successful scoped rows; do not repeat them as blanket passes |
| P2, G11 — enemy terrain navigation | Horizontal root sweep keeps fixed Y for many families; raised steps/gaps exist | Choose explicit grounded/hopping/flying/burrowing traversal policies. Reproduce crab/pier, gator/dock and humanoid/pipe cases. Require no indefinite stuck encounter and successful ordinary combat completion |
| P2, G12 — repeated flinch lock | Every ordinary hit refreshes0.18s flinch; fast recoveries can repeat more quickly | Measure accepted hit rate, stagger uptime and whether miniboss attacks resolve. If needed add a bounded elite stagger rule with a visible cue; preserve intended small-enemy responsiveness |
| P2, G15 — visible silhouette versus hitbox | Models extend beyond authoritative root; false weak-point affordance was muted | Produce actual-engine contact sheets with colliders. Attack limbs/torso/armor/empty space from both sides; document intended hit acceptance |
| P2, G16 — cost of world/server animation/support scans | Counts are bounded in parts of the runtime, but worst-case CPU/network/memory has not been measured | Profile worst stage and ten resets. Optimize a measured cost; avoid a speculative rewrite or claiming pooled lifetimes from historical docs |
| P2 — latency/physics tolerance | Envelope permits finite replication uncertainty, not arbitrary network delay | Trace normal/jump/dodge/cart/pad/wind at representative latency and hitches; verify corrections neither grant extra travel nor reject legal motion |
| P2 — device/modal/asset acceptance | Pure layout and device rules cannot show actual text, input routing or rig animation | Execute phone portrait/landscape, controller-only full-bag reward, hybrid switching and R6/R15/denied clip cases |
| P2 — save/provider deployment gates | Local QA is deliberately non-writable; provider guarantees have not been exercised | Use an approved controlled test universe/profile fixture for durability, migration, lease conflicts, response loss and shutdown; preserve production records |
| P3 — item/full-bag explanation and art variety | Quiet drop decline and generic held-weapon silhouettes remain | Observe a real user's recognition; add concise truthful feedback or visual distinction only where the interaction fails |
| Documentation — historical claims and ongoing evidence | Current ledger separates 09 static evidence from 08 runtime; 22 unsafe historical generator entrypoints are now retired with unchanged bytes | Use scripts/validate.py for current evidence. Keep obsolete audio/provider/publish suggestions subordinate to this audit's no-audio and no-publish constraints |

## 7. Completion plan with ordering, owners and exit criteria

### Pass A — finish the user's opening flow on the exact build

**Owner:** root runtime/client integration. **Source foundation:** main09/QA09 passed separate pipelines; latest runtime source remains 08. Its Hub Tab, Input.Intent movement/jump, native NPC E and client silence scan are established only in their scoped cases. Dialogue 09 engine reading remains the first unexecuted repair check.

1. Verify the package identity in Options/log and capture it with the test record.
2. Cold join → ready → E at bonfire → Daytona; Return → E beside Steve → shop → close → start again.
3. Preserve Hub Tab open/close evidence and test it from Active/required menus on the next identified build. Record O's Studio conflict; test D-pad-left and confirm no required Draft can be replaced.
4. Preserve the native05 Space and08 Input.Intent movement/short-release jump cases. Numerically test native walking controls and same-build held versus released jump, landing and accepted combat actions; extend to R6/R15 and delayed assembly.
5. Recheck all above after death/replacement. No successful-looking animation or message may substitute for accepted state.

**Exit:** all actions have matching-build evidence, zero unexplained startup/input errors, no permanent loading state and no repeat confirmation/prompt request for an already accepted action.

### Pass B — run isolated composition checks, then real mechanic regressions

**Owner:** root runtime with gameplay/movement owners fixing reproduced defects.

The latest separately built QA09 contains the allowlisted server BindableFunction bridge and client BindableFunction diagnostics. Neither runs tests automatically or appears in default.project.json. Server mutations enforce Studio/server/PlaceId0, admitted owner, loaded non-writable profile and ready living character. Each endpoint serializes its commands; the client path still uses ordinary server authorization. No network development remote is added.

Use explicit service smoke to exercise wave/kill/rescue/reward callbacks, generation cancellation and repeated completion. Preserve the helper's actual returned assertions and truncation/error status. Reset follows normal hub→fresh run→target-stage transitions so run items and reward ledgers reset; persistent loadout is retained.

The executed QA06 support assertion remains unresolved as an acceptance case. Main08 contains bootstrap/spawn cleanup, but QA08 stage/lifecycle/Active-combat checks were not started because commit headroom was 1.45–1.61 GiB. On a host with sustained headroom, rerun and preserve each stage record plus actual first support hit rather than a truncated aggregate. The earlier ownership failure does not establish absent physical floor. Trace captures bounded 30-second positions/command receipts, not accepted combat. Lifecycle smoke still lacks a recorded success and cannot establish rejoin durability.

Then exercise the actual player paths: denied/accepted actions, short enemies hit by coil/projectiles, delayed healing without more input, mixed hazards, corrected-position damage, missing Cold One, Barge gap, locked-gate dashes and full-bag Draft retry.

**Exit:** the service smoke and each physical regression have separate rows. A teleport/lethal-test-damage smoke is never labelled a normal campaign or difficulty test.

### Pass C — traverse the campaign and measure combat

**Owner:** gameplay/world/balance, with root engine evidence.

Traverse the twenty-stage route continuously using ordinary controls and record objectives, encounter completion, rewards and errors. Cover starter, slow heavy, ranged/thrown, high-mobility, defensive and weak-draft cases across the campaign. Test both finale objective orders independently and duplicate completion attempts.

Log unreachable objects, enemy stuck coordinates, stage time, accepted attack rate, real time-to-kill, player damage and uninterrupted flinch. Fix terrain navigation or animation/hit-geometry defects before retuning HP to disguise them.

**Exit:** no progression softlock; required objectives correctly reject and later accept; no old-generation attack/reward survives; finale and later-run unlocks behave once as designed.

### Pass D — visual, device, silence and accessibility acceptance

**Owner:** client/art/accessibility, with actual device capture.

Compare the same hub camera in the updated lighting build. Check all enemy fronts/colliders, held weapons, attacks/skills, timing cues and stage lighting; certify only actual rendered assets. Inspect low/high graphics, normal/Large text, Slow/Instant reading, Reduce motion/flashes and patterned danger cues.

Exercise keyboard, controller, touch and hybrid-device switching. Required menus must be usable without a mouse. First verify repaired startup dialogue retains full text and paginates at a valid viewport; pages must remain legible and outside Jump/USE hit areas. Supplement the observed 05 server / 08 client audio samples with all verbs/stages and explicit experience voice-configuration evidence.

**Exit:** usable visual hierarchy on supported devices, complete silent necessary dialogue, no false danger/weak-point cues, no unsupported essential glyph and no focus deadlock.

### Pass E — persistence, admission, performance and release evidence

**Owner:** profile/security/deployment/runtime.

Use controlled non-production records for real provider success/failure/rejoin tests. Test revision/lease conflicts, retry after response loss, migration/future data, queued mutation during write and shutdown. Verify failed loads remain non-writable and progression/status is truthful.

Test a second simultaneous join and verify rejection precedes profile/world creation. Set and verify deployed MaxPlayers1 only through the deployment owner's authorized configuration workflow. This game does not promise cooperative multiplayer.

Profile client and server under the worst encounter and across repeated deaths/runs; record stable instance/task/connection/queue counts, heap, frame time and replicated work. Server heartbeat samples are not client FPS. Verify analytics delivery separately and keep it non-authoritative.

**Exit:** every release-blocking gate below has evidence tied to the intended artifact/configuration. Reconcile README/release docs and preserve the previous known artifact/rollback procedure before any later publish decision.

## 8. Release acceptance matrix

| Gate | Evidence required to close it | Current state |
|---|---|---|
| Source compile/types/contracts | Current immutable report, all mapped source/tests, no errors/stable inputs | **Passed main09: 78 runtime files, 112 checks; sourceStable=true** |
| Behavioral rule suites | Actual production rules, positive/negative/failure cases, recorded execution | **Passed 15 suites in main09**, including production dialogue pagination; no engine inference |
| QA packaging/isolation | Matching baseline, helper/bridge/client hashes, guards, derived project only | **Passed QA09 six checks.** Actual bridge/client invocations belong to 08/earlier; no 09 runtime result |
| Engine boot and opening controls | Fresh/late character, bonfire/NPC, shortcuts, camera, no errors on intended build | **Partial03/05/08 evidence; 08 Hub Tab and native NPC E after walking observed.** No 09 engine check; Active/device/delayed-assembly matrix open |
| Physical movement/combat | Jump/landing, legal/rejected actions, collision, enemy terrain, accepted feedback | **03 dodge,05 native jump and08 Input.Intent walk/jump observed.**08 Hub combat unavailable/not attempted; native movement-device and Active matrices open |
| Campaign/objectives/finale | Ordinary twenty-stage run plus both finale orders, retries and later-run rewards | **Open** |
| Device/UI/accessibility/silence | Keyboard/controller/touch captures, fonts/focus, silence including experience configuration | **Open.**08 dialogue defect fixed/tested in 09 source only; clean bounded 05 server/08 client audio scans. Voice, complete conversations/device/visual acceptance open |
| Assets and animation permissions | Real R6/R15/procedural and denied/partial imported tests; approved visual/contact-sheet results | **Open** |
| Save durability and analytics delivery | Controlled live provider/rejoin/failure/shutdown records, no production rewrite | **Open** |
| Admission/deployment model | Second join rejected early; deployed MaxPlayers1; correct permissions/voice/settings | **Open** |
| Soak/performance | Worst-stage client/server timing, memory/network and stable reset baselines on target devices | **Open** |
| Security source coverage | Complete intended source scope, hashed reviewed versions, findings/limits and exclusions | **Sealed: 138 paths, including 112 authoritative/78 runtime; no additional reportable finding.** Zero unreviewed-authoritative/unclassified remainder. Engine/provider/deployment/device gates remain separate |
| Release provenance/rollback/docs | Exact package/revision/source manifest, verified config, prior artifact and current truthful docs | **Build provenance improved; release handoff open** |
| Publishing | Separately authorized release after all blocking acceptance is established | **Not performed in this audit** |

## 9. Resource policy and collaboration

Use one existing Studio instance and serial compile/type/package work. Do not run another engine/build in parallel to accelerate a checklist. Earlier resource pressure and current usable-frame evidence both remain part of the record; neither proves target-device performance.

The host policy in RELEASE_ACCEPTANCE stops additional heavy work below 512 MiB free physical memory or below 2 GiB commit headroom. During QA08, commit headroom repeatedly measured 1.45–1.61 GiB. Stage smoke, lifecycle smoke and Active combat smoke were therefore **not executed**. The root closed its own Studio instance; headroom recovered to about 5.0 GiB with about 1.9 GiB physical free. One play session consumed roughly 3.5 GiB of commit headroom, so the recovered idle reading alone does not make another heavy playtest safe. These are real host constraints, not target-device performance results. Do not change paging/security/privacy/provider settings or terminate unrelated apps. Preserve recovery files; resume heavier acceptance only with sustainable capacity.

Source ownership during this mission is partitioned: root composes startup/progression/runtime QA and runs the engine; client owner changes client controllers/UI and corresponding pure tests; gameplay owner changes combat/world/content; movement/profile/security boundaries are coordinated explicitly. Freeze source during validator reads and record any post-hold correction. Documentation work may continue while source is held. No extra subagents or heavy background processes are needed for this report.

## 10. Required final runtime update

Before this audit can support completion, the root reviewer should update the ledger with actual new results: package identity, action/assertion, expected versus actual outcome, trace/screenshot/log path, and remaining failure or limitation. Update source/QA hashes if any fix followed a failed test.

Main09/QA09 pass their recorded static pipelines, including the new dialogue rule suite. The latest engine record remains 08 and contains both scoped successes and the reproduced dialogue defect; 09's repair has no engine acceptance. Stage/lifecycle/Active-combat checks were not run on 08 because commit headroom fell below the operating threshold; ordinary campaign, device/provider/deployment and performance gates remain open. The task's own Studio is closed. Preserve immutable evidence and complete runtime work only with sustainable memory headroom; do not describe the mission or release as complete.
